version: "3.3"

services:
    mysql:
        image: mysql
        command: --default-authentication-plugin=mysql_native_password
        restart: always
        ports:
            - "3312:3306"
        environment:
            MYSQL_ROOT_PASSWORD: root
