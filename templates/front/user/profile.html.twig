{% extends 'front/front.html.twig' %}

{% block title %}Library Shop - {{ app.user.fullName }}{% endblock %}

{% block content %}
<h1>Votre profile</h1>

<p>
    <a href="{{ path('app_front_home_index') }}">Retour</a>
</p>

<ul>
    <li>Nom : {{ app.user.lastname }}</li>
    <li>Prenom : {{ app.user.firstname }}</li>
    <li>email : {{ app.user.email }}</li>
</ul>

<h2>Vos livres</h2>

{% if app.user.books.count == 0 %}
    <p>Vous n'avez pas encore de livre(s) à revendre</p>
{% else %}
<div class="grid">
    {% for book in app.user.books %}
        {% include 'front/partials/_bookThumb.html.twig' %}
    {% endfor %}
</div>
{% endif %}


{% endblock %}

